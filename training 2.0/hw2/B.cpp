#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main() {
    int n = 10;
    vector<int> houses{}, stores{};

    for (int i = 0; i < n; ++i) {
        cin >> elem;
        if (elem == 1)
            stores.push_back(i);
        if (elem == 2)
            houses.push_back(i);
        ++i;
    }

    for (auto &house : houses) {
        if (house > right)

    }
}
