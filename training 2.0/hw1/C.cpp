#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main() {
    int x, y, tmp;
    cin >> x >> y >> tmp;
    
    // . . . a . . b . . n
    cout << (x > 12 || y > 12 || x == y);
}
